// 1. 열거형이란
// - 먼저 enum 키워드 뒤에 열거형 이름을 사용한다.
// - 다음 열거형에 대한 상수 값을 정의한다.

// enum name {constant1, constant2, ...};
// constant1, constant2 등은 열거형의 멤버라고도 한다.

enum Month {
    Jan,
    Feb,
    Mar,
    Apr,
    May,
    Jun,
    Jul,
    Aug,
    Sep,
    Oct,
    Nov,
    Dec
};
